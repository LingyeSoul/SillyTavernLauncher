/**
 * 启动器版本（← src/version.py 的等价物）。
 * D5：展示前经 services/updater.normalizeVersion 规范化为 semver pre-release 形态
 * （如 'v2.0.0-alpha.0'）；与 package.json version 保持同步。
 */
export const APP_VERSION = 'v2.0.0-beta.2'
